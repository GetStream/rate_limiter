name: example
description: A new Flutter project.

publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  rate_limiter: ^0.1.0

dependency_overrides:
  rate_limiter:
    path: ../
